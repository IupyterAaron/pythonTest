K-means 聚类算法
1. 什么是聚类？
    把相似的东西分到一组
2. 如何计算相似度？
    欧氏距离        空间之间的直线距离
    曼哈顿距离      “城市街区距离”，从一个十字路口到另一个十字路口的距离
    余弦距离        用向量空间中的两个向量夹角的余弦值作为衡量两个样本差异的大小
3. 归一化
    在计算相似度过程中，对于特征向量中含有的不同量级的数据应先将数据归一化，是否归一化会导致聚类结果明显不同
4. K-Means

